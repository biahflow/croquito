# Contratos compartilhados

`scene.schema.json` e `src/scene.generated.ts` são artefatos gerados a partir de
`croquito_core.models.SceneRevision`. Não os edite manualmente.

Use `make contracts` para regenerar e `make check` para detectar divergências.

