BUILD REPORT

Status: BUILDER_VALIDATION_BLOCKED
Files changed: `scripts/smoke_hml.py` (checks novos de `/` e `/login`, sem alterar os quatro checks existentes); `apps/web/e2e/smoke-headless.mjs` (clique no CTA `Entrar` ancorado em `main.login`, com as esperas e asserções existentes preservadas); `docs/features/F-007-tela-de-login/tasks/T5-build-report.md` (este relatório obrigatório).
Validation executed: pre-flight de READ/WRITE/VALIDATE e Chromium concluído; árvore inicialmente limpa; baseline `make check` verde; baseline `make test` verde (1.438 passed, 10 skipped; web 529 passed); `git diff --check` verde; revisão do diff confirmou os quatro checks antigos intactos e nenhuma espera/asserção removida do e2e; final `make check` verde; final `make test` verde (1.438 passed, 10 skipped; web 529 passed); `make dev-services` executado e bloqueado com exit 2 porque `0.0.0.0:5432` já estava publicado pelo container alheio `plataforma-oikosapagar-postgres-1`; `make down-services` verde e removeu os containers e a rede do Croquito, preservando o container alheio; estado final com somente os três arquivos declarados neste relatório e nenhum commit criado.
Validation skipped: `make db-init`, `make dev-api`, web dev server, `CROQUITO_ALLOW_TEST_TOKENS=true make smoke-local` e execução de `apps/web/e2e/smoke-headless.mjs` com `?job=<uuid>`; todos dependiam do PostgreSQL do stack local que não pôde iniciar na porta fixa 5432. Nenhum desses checks foi tratado como opcional.
Unavailable capabilities: `VALIDATE` para o stack local completo; a porta fixa `127.0.0.1:5432` do `docker-compose.local.yml` estava indisponível e liberar a porta exigiria interromper um container de outro projeto, fora do escopo autorizado.
Assumptions: T1–T4 estavam integradas na branch, conforme informado pelo usuário; o container `plataforma-oikosapagar-postgres-1` pertence a outro projeto e não podia ser parado implicitamente; o artefato de reporting exigido pelo Task Contract é uma exceção explícita ao WRITE restrito aos dois arquivos de implementação.
Remaining risks: os dois smokes obrigatórios não foram executados contra o stack real; portanto a resposta 302 de `/`, o conteúdo SPA de `/login`, a passagem pelo tema do Keycloak e a preservação real de `?job` ainda não têm evidência final desta execução. A implementação permanece candidata, não concluída.
Human decisions required: liberar a porta 5432 sem afetar trabalho alheio, autorizar uma janela para parar temporariamente o container que a ocupa ou fornecer um ambiente isolado equivalente; depois, rerodar `make dev-services`, `make db-init`, `make dev-api`, o web dev server, `CROQUITO_ALLOW_TEST_TOKENS=true make smoke-local` e o smoke headless com `?job=<uuid>`.
